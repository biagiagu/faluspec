{
  "name": "faluspec",
  "version": "0.1.0",
  "description": "Valida un backlog FaLuSpec y genera sus vistas",
  "type": "module",
  "bin": { "faluspec": "./dist/cli.js" },
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
